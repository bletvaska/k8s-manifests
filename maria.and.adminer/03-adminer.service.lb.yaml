apiVersion: v1
kind: Service
metadata:
  labels:
    app: adminer
  name: adminer-lb
  namespace: the-project
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8080
  selector:
    app: adminer
  type: LoadBalancer
