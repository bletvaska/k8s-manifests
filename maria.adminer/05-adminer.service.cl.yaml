apiVersion: v1
kind: Service
metadata:
  labels:
    app: adminer
  name: adminer-cl
  namespace: the-project
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: adminer
