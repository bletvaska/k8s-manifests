apiVersion: v1
kind: Service
metadata:
  labels:
    app: adminer
  name: adminer-lb
  namespace: the-project
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8000
  selector:
    app: adminer
  type: LoadBalancer
