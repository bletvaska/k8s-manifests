apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb
  # namespace: application
spec:
  # storageClassName: "local-storage"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi
