apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claim-200
  # namespace: application
spec:
  storageClassName: "local-storage"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
